import React, { useMemo, useState } from "react";
export default function FinancialStartPicker({ customers = [], vehicles = [], onSelect, onClose }) {
  const [customerId, setCustomerId] = useState(""), [vehicleId, setVehicleId] = useState("");
  const available = useMemo(() => vehicles.filter(v => v.customer_id === customerId), [vehicles, customerId]);
  return <div className="fixed inset-0 z-[10020] flex items-center justify-center bg-black/80 p-4"><div role="dialog" aria-modal="true" className="w-full max-w-md rounded-2xl border border-gray-800 bg-gray-950 p-6"><h2 className="text-xl font-bold text-white">Start Invoice</h2><p className="mt-1 text-sm text-gray-400">Choose the customer and vehicle for this job.</p><label className="mt-5 block text-sm text-gray-400">Customer<select value={customerId} onChange={e => { setCustomerId(e.target.value); setVehicleId(""); }} className="mt-1 h-10 w-full rounded-lg border border-gray-700 bg-gray-900 px-3 text-white"><option value="">Select customer</option>{customers.map(c => <option key={c.id} value={c.id}>{c.full_name}</option>)}</select></label><label className="mt-4 block text-sm text-gray-400">Vehicle<select value={vehicleId} onChange={e => setVehicleId(e.target.value)} className="mt-1 h-10 w-full rounded-lg border border-gray-700 bg-gray-900 px-3 text-white" disabled={!customerId}><option value="">Select vehicle</option>{available.map(v => <option key={v.id} value={v.id}>{v.year} {v.make} {v.model}</option>)}</select></label><div className="mt-6 flex gap-3"><button onClick={onClose} className="flex-1 rounded-lg border border-gray-700 px-4 py-2 text-gray-300">Cancel</button><button disabled={!vehicleId} onClick={() => onSelect({ type: "vehicle", id: vehicleId })} className="flex-1 rounded-lg bg-cyan-600 px-4 py-2 font-semibold text-white disabled:opacity-50">Continue</button></div></div></div>;
}